<img srcset="{% render 'img-srcset' with image: image %}"
  sizes="{{ sizes | default: '(min-width: 750px) 50vw, 100vw' }}"
  alt="{{ image.alt | escape }}"
  width="{{ image.width }}"
  height="{{ image.height }}"
  class="{{ css_classes }}"
>