{%- liquid
  if template.suffix != 'quick_view'
    render 'breadcrumbs'
    assign layout_name = section.settings.layout
  else
    assign layout_name = 'normal'
  endif
-%}
{%- if template.suffix != 'quick_view' or request.design_mode -%}
<div class="product-template-section container{% render 'wide-css-class' %} big-gutter-bottom" id="ProductSection-{{ section.id }}" data-section-id="{{ section.id }}" data-section-type="product" data-enable-history-state="true">
{%- endif -%}
  {%- assign current_variant = product.selected_or_first_available_variant -%}
  {%- assign featured_media = product.selected_or_first_available_variant.featured_media | default: product.featured_media -%}
  {%- capture description -%}
    <div class="product-single__description rte{% if template.suffix == 'quick_view' %} product-single__description--quickview custom-scrollbar{% endif %}"{% if template.suffix == 'quick_view' %} style="padding-right:5px;"{% endif %}>
      {{ product.description | replace: '<img src=','<img loading="lazy" src=' }}
    </div>
  {%- endcapture -%}
  {%- liquid
    assign sizechart_vendor_key = 'sc_vender_' | append: product.vendor | downcase | replace: ' ','_'
    assign sizechart_type_key = 'sc_vender_' | append: product.type | downcase | replace: ' ','_'
    if template.suffix != 'quick_view'
      if product.metafields.btt.size_chart != blank
        assign current_sizechart = product.metafields.btt.size_chart
      elsif product.metafields.btt[sizechart_type_key] != blank  
        assign current_sizechart = product.metafields.btt[sizechart_type_key]
      elsif product.metafields.btt[sizechart_vendor_key] != blank  
        assign current_sizechart = product.metafields.btt[sizechart_vendor_key]  
      elsif collection and collection.metafields.btt.size_chart and collection.metafields.btt.apply_size_chart_product
        assign current_sizechart = collection.metafields.btt.size_chart 
      elsif product.collections[0] and product.collections[0].metafields.btt.size_chart and product.collections[0].metafields.btt.apply_size_chart_product
        assign current_sizechart = product.collections[0].metafields.btt.size_chart  
      endif
    endif
    unless settings.enable_rtl and settings.rtl_language_codes contains request.locale.iso_code
      assign left_holder = 'left'
      assign right_holder = 'right'
      assign enable_rtl = false
    else
      assign left_holder = 'right'
      assign right_holder = 'left'
      assign enable_rtl = true
    endunless
    assign product_form_id = 'product-form-' | append: section.id
    assign has_popup_content = false
    assign has_quantity_box = false
    assign is_show_desc = false
    assign inline_button = true
    if layout_name == 'ver-images-scrolling'
      assign inline_button = false
    endif
    assign single_main_size = '(min-width: 992px) 50vw, 100vw'
    if layout_name != 'normal' or section.settings.large_images_grid
      assign single_main_size = '50w'
    endif
  -%}
  <div class="product-single pg row" data-product-handle="{{ product.handle }}" data-zoom-scope="{% if template.suffix == 'quick_view' %}quick_view{% else %}single{% endif %}" data-ignore-sold-out-text="1"{% if template.suffix != 'quick_view' %} data-history="1"{% endif %}{% if template.suffix != 'quick_view' %} data-sync-pg=".sc"{% endif %}>
    {%- if layout_name == 'normal' -%}
      <div class="col-xs-12 {% unless section.settings.large_images_grid %}col-md-6{% else %}col-md-7 no-gutter-left-mb no-gutter-right-mb{% endunless %}{% if enable_rtl %} f-right{% endif %}{% if section.settings.sticky_content == 'images' %} pos-sticky pos-sticky--{{ section.id }}{% endif %}{% if section.settings.large_images_grid %} gutter-ele-bottom-tbs{% endif %}">
        {%- render 'product-single-media' with product: product, featured_media: featured_media, large_images_grid: section.settings.large_images_grid, enable_rtl: enable_rtl, single_main_size: single_main_size -%}
      </div>
      <div class="col-xs-12 {% unless section.settings.large_images_grid %}col-md-6{% else %}col-md-5{% endunless %} gutter-ele-top-tbs{% if section.settings.sticky_content == 'information' %} pos-sticky pos-sticky--{{ section.id }}{% endif %}">
        {%- for block in section.blocks -%}
          {%- unless block.type == '@app' -%}
            {%- if block.type == 'size_chart' -%}
              {%- if template.suffix != 'quick_view' -%}
                {%- liquid
                  unless current_sizechart != blank
                    if block.settings.use_size_chart_fallback and block.settings.size_chart_fallback != blank
                      assign sizechart = block.settings.size_chart_fallback | img_url: 'master' | img_tag: block.settings.size_chart_fallback.alt, 'img-responsive'
                    elsif product.description contains 'sizechart-table'
                      assign sizechart = 'inline'    
                    endif
                  else
                    assign sizechart = current_sizechart
                  endunless
                -%}
                {%- render 'product-single-block-list' with current_variant: current_variant, block: block, sizechart: sizechart, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
              {%- endif -%}
            {%- else -%}
              {%- if block.type == 'popup' -%}
                {%- if template.suffix != 'quick_view' -%}
                  {%- assign has_popup_content = true -%}
                  {%- assign popup_title = block.settings.page.title -%}
                  {%- assign popup_content = block.settings.page.content -%}  
                {%- endif -%}
              {%- elsif block.type == 'collapsible_tab_desc' or block.type == 'description' -%}
                {%- assign is_show_desc = true -%}
              {%- elsif block.type == 'quantity_buy_buttons' -%}
                {%- assign has_quantity_box = block.settings.show_quantity_selector -%}
              {%- endif -%}  
              {%- render 'product-single-block-list' with current_variant: current_variant, block: block, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder, product_form_id: product_form_id, description: description, inline_button: inline_button -%}  
            {%- endif -%}
          {%- else -%}
            {%- render block -%}
          {%- endunless -%}
        {%- endfor -%}
        {%- if template.suffix == 'quick_view' and is_show_desc == false -%}
          <div class="gutter-ele-top gutter-ele-bottom">{{ description }}</div>
        {%- endif -%}
        {%- render 'product-single-view-details' -%}
      </div>
    {%- elsif layout_name == 'ver-images-scrolling' -%}
      {%- assign youtubeCapture = '' -%}
      {%- assign vimeoCapture = '' -%}
      <div class="col-xs-12 col-md-8{% if enable_rtl %} f-right{% endif %}">
        <div class="row">
          <div class="col-xs-12 col-md-6{% unless enable_rtl %} f-right{% endunless %} no-gutter-left-mb no-gutter-right-mb gutter-ele-bottom-tbs">
            <div class="product-single__photos pg__mains scroll-touch-mobile-main pg__mains--grid por custom-scrollbar">
              {%- assign single_custom_class = 'grid__item grid__item--media por no-gutter' -%}
              {%- render 'product-single-main' with product: product, media: featured_media, lazyload: false, use_slider: false, no_gutter: true, custom_class: single_custom_class, show_media_btn_inner: true, is_active: true, single_main_size: single_main_size -%}
              {%- for media in product.media -%}
                {%- if media != featured_media -%}
                  {%- render 'product-single-main' with product: product, media: media,, lazyload: true, use_slider: false, custom_class: single_custom_class, no_gutter: true, show_media_btn_inner: true, is_active: false, single_main_size: single_main_size -%}
                {%- endif -%}
                {%- if media.media_type == 'external_video' -%}
                  {%- if media.host == 'youtube' -%}
                    {%- capture yt -%}{%- render 'product-single-capture-youtube' with product: product, media: media, ratio: ratio -%}{%- endcapture -%}
                    {%- assign youtubeCapture = youtubeCapture | append: yt -%}
                  {%- elsif media.host == 'vimeo' -%}
                    {%- capture vim -%}{%- render 'product-single-capture-vimeo' with product: product, media: media, video_id: media.external_id -%}{%- endcapture -%}
                    {%- assign vimeoCapture = vimeoCapture | append: vim -%}  
                  {%- endif -%}
                {%- endif -%}
              {%- endfor -%}
              {{ youtubeCapture }}
              {{ vimeoCapture }}
            </div>
          </div>
          {%- assign block_size = section.blocks | size -%}
          {%- if block_size > 0 -%}
          <div class="col-xs-12 col-md-6 pos-sticky pos-sticky--{{ section.id }} flex flex-align-ver gutter-ele-top-tbs">
            <div class="w100">
              {%- assign limit_1 = block_size | plus: 0.00 | divided_by: 2 | ceil | at_most: 5 | minus: 1 -%}
              {%- for i in (0..limit_1) -%}
                {%- assign block = section.blocks[i] -%}
                {%- unless block.type == '@app' or block.type contains 'collapsible' -%}
                  {%- if block.type == 'size_chart' -%}
                    {%- if template.suffix != 'quick_view' -%}
                      {%- liquid
                        unless current_sizechart != blank
                          if block.settings.use_size_chart_fallback and block.settings.size_chart_fallback != blank
                            assign sizechart = block.settings.size_chart_fallback | img_url: 'master' | img_tag: block.settings.size_chart_fallback.alt, 'img-responsive'
                          elsif product.description contains 'sizechart-table'
                            assign sizechart = 'inline'    
                          endif
                        else
                          assign sizechart = current_sizechart
                        endunless
                      -%}
                      {%- render 'product-single-block-list' with current_variant: current_variant, block: block, sizechart: sizechart, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
                    {%- endif -%}
                  {%- else -%}
                    {%- if block.type == 'popup' -%}
                      {%- if template.suffix != 'quick_view' -%}
                        {%- assign has_popup_content = true -%}
                        {%- assign popup_title = block.settings.page.title -%}
                        {%- assign popup_content = block.settings.page.content -%}  
                      {%- endif -%}
                    {%- elsif block.type == 'collapsible_tab_desc' or block.type == 'description' -%}
                      {%- assign is_show_desc = true -%}
                    {%- elsif block.type == 'quantity_buy_buttons' -%}
                      {%- assign has_quantity_box = block.settings.show_quantity_selector -%}
                    {%- endif -%}  
                    {%- render 'product-single-block-list' with current_variant: current_variant, block: block, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder, product_form_id: product_form_id, description: description, inline_button: inline_button -%}  
                  {%- endif -%}
                {%- elsif block.type == '@app' -%}
                  {%- render block -%}
                {%- endunless -%}
              {%- endfor -%}
            </div>
          </div>
          {%- endif -%}
        </div>
      </div>

      <div class="col-xs-12 col-md-4 gutter-ele-top-tbs pos-sticky pos-sticky--{{ section.id }} flex flex-align-ver">
        <div class="w100">
          {%- if block_size > 0 and limit_1 < block_size -%}
            {%- assign limit_1 = limit_1 | plus: 1 -%}
            {%- assign block_size = block_size | minus: 1 -%}
            {%- for i in (limit_1..block_size) -%}
              {%- assign block = section.blocks[i] -%}
              {%- unless block.type == '@app' or block.type contains 'collapsible' -%}
                {%- if block.type == 'size_chart' -%}
                  {%- if template.suffix != 'quick_view' -%}
                    {%- liquid
                      unless current_sizechart != blank
                        if block.settings.use_size_chart_fallback and block.settings.size_chart_fallback != blank
                          assign sizechart = block.settings.size_chart_fallback | img_url: 'master' | img_tag: block.settings.size_chart_fallback.alt, 'img-responsive'
                        elsif product.description contains 'sizechart-table'
                          assign sizechart = 'inline'    
                        endif
                      else
                        assign sizechart = current_sizechart
                      endunless
                    -%}
                    {%- render 'product-single-block-list' with current_variant: current_variant, block: block, sizechart: sizechart, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
                  {%- endif -%}
                {%- else -%}
                  {%- if block.type == 'popup' -%}
                    {%- if template.suffix != 'quick_view' -%}
                      {%- assign has_popup_content = true -%}
                      {%- assign popup_title = block.settings.page.title -%}
                      {%- assign popup_content = block.settings.page.content -%}  
                    {%- endif -%}
                  {%- elsif block.type == 'collapsible_tab_desc' or block.type == 'description' -%}
                    {%- assign is_show_desc = true -%}
                  {%- elsif block.type == 'quantity_buy_buttons' -%}
                    {%- assign has_quantity_box = block.settings.show_quantity_selector -%}
                  {%- endif -%}  
                  {%- render 'product-single-block-list' with current_variant: current_variant, block: block, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder, product_form_id: product_form_id, description: description, inline_button: inline_button -%}  
                {%- endif -%}
              {%- elsif block.type == '@app' -%}
                {%- render block -%}
              {%- endunless -%}
            {%- endfor -%}
          {%- endif -%}
        </div>
      </div>
    {%- elsif layout_name == 'hoz-images-scrolling' -%}
      {%- assign youtubeCapture = '' -%}
      {%- assign vimeoCapture = '' -%}
      <div class="col-xs-12">
        <div class="row flex"{% unless enable_rtl %} style="flex-flow:row-reverse wrap;"{% endunless %}>
          <div class="col-xs-12 col-sm-6 col-md-8{% unless enable_rtl %} f-right{% endunless %} no-gutter-left-mb no-gutter-right-mb gutter-ele-bottom-tbs">
            <div class="product-single__photos pg__mains pg__mains--scroll-hoz scroll-main-hoz por custom-scrollbar flex scroll-touch-mobile-main">
              {%- assign single_custom_class = 'pg__main--scroll-hoz por no-gutter' -%}
              {%- if product.media.size == 1 -%}
                {%- assign single_custom_class = single_custom_class | append: ' pg__main--scroll-hoz--full-mobile' -%}
              {%- endif -%}
              {%- render 'product-single-main' with product: product, media: featured_media, lazyload: false, use_slider: false, no_gutter: true, custom_class: single_custom_class, show_media_btn_inner: true, is_active: true, single_main_size: single_main_size -%}
              {%- for media in product.media -%}
                {%- if media != featured_media -%}
                  {%- render 'product-single-main' with product: product, media: media, lazyload: true, use_slider: false, no_gutter: true, custom_class: single_custom_class, show_media_btn_inner: true, is_active: false, single_main_size: single_main_size -%}
                {%- endif -%}
                {%- if media.media_type == 'external_video' -%}
                  {%- if media.host == 'youtube' -%}
                    {%- capture yt -%}{%- render 'product-single-capture-youtube' with product: product, media: media, ratio: ratio -%}{%- endcapture -%}
                    {%- assign youtubeCapture = youtubeCapture | append: yt -%}
                  {%- elsif media.host == 'vimeo' -%}
                    {%- capture vim -%}{%- render 'product-single-capture-vimeo' with product: product, media: media, video_id: media.external_id -%}{%- endcapture -%}
                    {%- assign vimeoCapture = vimeoCapture | append: vim -%}  
                  {%- endif -%}
                {%- endif -%}
              {%- endfor -%}
              {{ youtubeCapture }}
              {{ vimeoCapture }}
            </div>
          </div>
          <div class="col-xs-12 col-sm-6 col-md-4 flex flex-align-ver gutter-ele-top-tbs">
            {%- assign block_size = section.blocks | size -%}
            {%- assign hidden_blocks = '' -%}
            {%- if block_size > 0 -%}
              {%- assign opened_tag = true -%}
              {%- assign index = 1 -%}
              {%- assign block_size = block_size | minus: 1 -%}
              {%- assign limit_block_1 = block_size | at_most: 6 -%}
              {%- assign start_block_2 = limit_block_1 | plus: 1 -%}
              <div class="w100">
              {%- for block in section.blocks -%}
                {%- unless block.type == 'variant_picker' or block.type == 'quantity_buy_buttons' or block.type contains 'collapsible' -%}
                  {%- if index == 2 -%}
                    </div>
                    <div class="w100">
                    {%- assign opened_tag = true -%}
                  {%- endif -%}
                  {%- if index == start_block_2 -%}
                    </div>
                    <div class="hidden-xs hidden-sm w100">
                    {%- assign opened_tag = true -%}  
                  {%- endif -%}
                  {%- capture block_html -%}
                    {%- unless block.type == '@app' or block.type contains 'collapsible' -%}
                      {%- if block.type == 'size_chart' -%}
                        {%- if template.suffix != 'quick_view' -%}
                          {%- liquid
                            unless current_sizechart != blank
                              if block.settings.use_size_chart_fallback and block.settings.size_chart_fallback != blank
                                assign sizechart = block.settings.size_chart_fallback | img_url: 'master' | img_tag: block.settings.size_chart_fallback.alt, 'img-responsive'
                              elsif product.description contains 'sizechart-table'
                                assign sizechart = 'inline'    
                              endif
                            else
                              assign sizechart = current_sizechart
                            endunless
                          -%}
                          {%- render 'product-single-block-list' with current_variant: current_variant, block: block, sizechart: sizechart, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
                        {%- endif -%}
                      {%- else -%}
                        {%- if block.type == 'popup' -%}
                          {%- if template.suffix != 'quick_view' -%}
                            {%- assign has_popup_content = true -%}
                            {%- assign popup_title = block.settings.page.title -%}
                            {%- assign popup_content = block.settings.page.content -%}  
                          {%- endif -%}
                        {%- elsif block.type == 'collapsible_tab_desc' or block.type == 'description' -%}
                          {%- assign is_show_desc = true -%}
                        {%- elsif block.type == 'quantity_buy_buttons' -%}
                          {%- assign has_quantity_box = block.settings.show_quantity_selector -%}
                        {%- endif -%}  
                        {%- render 'product-single-block-list' with current_variant: current_variant, block: block, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder, product_form_id: product_form_id, description: description -%}  
                      {%- endif -%}
                    {%- elsif block.type == '@app' -%}
                      {%- render block -%}
                    {%- endunless -%}
                  {%- endcapture -%}
                  {{ block_html }}
                  {%- if index >= start_block_2 -%}
                    {%- assign hidden_blocks = hidden_blocks | append: block_html -%}
                  {%- endif -%}
                  {%- assign index = index | plus: 1 -%}
                {%- elsif block.type == 'variant_picker' or block.type == 'quantity_buy_buttons' -%}
                  {%- if block.type == 'variant_picker' -%}
                    {%- assign variant_picker_block = block -%}
                  {%- else -%}
                    {%- assign quantity_buy_buttons_block = block -%}  
                  {%- endif -%}
                {%- endunless -%}
              {%- endfor -%}
              {%- if opened_tag -%}
                </div>
              {%- endif -%}
            {%- endif -%}
          </div>
        </div>
      </div>

      {%- if quantity_buy_buttons_block != blank or variant_picker_block != blank -%}
      <div class="col-xs-12 gutter-top">
        <div class="pg__main-options pg__main-options--border flex flex-align-ver">
          {%- if variant_picker_block != blank -%}
            {%- unless product.has_only_default_variant -%}
            <div class="col-xs-12 col-md-4 pg__main-options__border-col">
              {%- render 'product-single-variants' with product: product -%}
            </div>
            {%- endunless -%}
          {%- endif -%}
          {%- if quantity_buy_buttons_block != blank -%}
          <div class="col-xs-12 col-md-8 pg__main-options__border-col">
            {%- render 'product-single-form' with product: product, current_variant: current_variant, full_qty_box: false, inline_button: true, left_holder: left_holder, obj: quantity_buy_buttons_block -%}
          </div>
          {%- endif -%}
        </div>
      </div>
      {%- endif -%}

      {%- if hidden_blocks != '' -%}
        <div class="col-xs-12 gutter-top hidden-md hidden-lg">
          <div class="pg__main-options--border">
            {{ hidden_blocks }}
          </div>
        </div>
      {%- endif -%}
    {%- endif -%}

    <script type="application/json" class="product-single-json" data-handle="{{ product.handle | escape }}" data-current-variant-id="{{ current_variant.id }}">
      {%- render 'product-tjson' with product: product -%}
    </script>
    <script type="application/json" id="ModelJson-{% if template.suffix != 'quick_view' %}{{ section.id }}{% else %}quickview-{{ product.handle | escape }}{% endif %}" class="model-json">
      {{ product.media | where: 'media_type', 'model' | json }}
    </script>
  </div>

{%- if template.suffix != 'quick_view' or request.design_mode -%}
  {%- if section.settings.sc_enable -%}
  {%- if current_variant.available -%}
    {%- unless settings.pre_order_enable and product.first_available_variant.inventory_policy == 'continue' and product.first_available_variant.inventory_quantity <= 0 -%}
      {%- assign cart_text = 'products.product.add_to_cart' | t -%}
    {%- else -%}
      {%- assign cart_text = 'products.product.pre_order' | t -%}
    {%- endunless -%}
  {%- else -%}
    {%- assign cart_text = 'products.product.sold_out' | t -%}  
  {%- endif -%}
  <div class="sc pg w100 hide" data-product-handle="{{ product.handle }}" data-ignore-sold-out-text="1" data-history="1" data-sync-pg=".product-single">
    {%- form 'product', product, data-productid: product.id, id: 'sc__form' -%}
<!-- Begin ReCharge code -->
{% if product.available %}{% render 'subscription-product' with product as product %}{% endif %}
<!-- End ReCharge code -->

      <div class="sc__inner container por center-block gutter-ele-top gutter-ele-bottom">
        <div class="row flex flex-align-ver">
          <div class="col-xs-3 col-sm-2 col-md-{%- unless product.has_only_default_variant -%}1{% else %}3{% endunless %} text-right hidden-xs hidden-sm">
            <div class="sc__image-wrap dib">
              <img data-src="{{ featured_media | img_url: '100x' }}"{% render 'img-one-pixel' %} 
                class="sc__image w100 pg__main-image img-responsive lazyload" 
                data-image-size="100x" 
                alt="{{ featured_media.alt | escape }}"/>
            </div>
          </div>
          <div class="col-xs-12 col-md-3 text-center tbs-text-left">
            {%- render 'product-single-variant-price' with current_variant as variant -%}
          </div>
          {%- unless product.has_only_default_variant -%}
          <div class="col-xs-12 col-md-3">
              {%- render 'product-group-variants' with product: product, enable_product_options: true, color_size: '60x', is_single_view: true, is_show_sub_info: false, is_single_sticky: true, force_dropdown_all: true -%}
          </div>
          {%- endunless -%}
          <div class="col-xs-12 col-md-{% unless product.has_only_default_variant %}5 sc__action-wrap{% else %}6{% endunless %}">
            <div class="sc__action text-left gutter-ele-top-tbs">
              <div class="product-form__item product-form__item--quantity flex w100">
                {%- if has_quantity_box -%}
                  <div class="qty-box qty-box--single qty-box--sticky flex flex-align-ver flex-align-space-between">
                    <button type="button" class="qty-box__ctrl qty-box__ctrl--descrease flex flex-align-hoz flex-align-ver" aria-label="button">
                      {%- render 'icon-minus' -%}
                    </button>
                    <input type="number" name="quantity" value="1" min="1" class="qty-box__input qty-box__input qty-box__input--sticky qty-box__input--sync qty-box__input--single w100 text-center h100" aria-label="form">
                    <button type="button" class="qty-box__ctrl qty-box__ctrl--increase flex flex-align-hoz flex-align-ver" aria-label="button">
                      {%- render 'icon-plus' -%}
                    </button>
                  </div>
                {%- endif -%}
                <button class="button button--cart add-cart-btn button--single-cart button--single-cart-sticky{% if product.has_only_default_variant %} button--single-cart-sticky-simple{% endif %} button--single-cart add-cart-btn--state button--one-line button--border-transparent por flex flex-align-ver flex-align-hoz" type="submit" name="add" title="{{ 'products.product.add_to_cart' | t | escape }}" aria-label="button" style="height:48px;"{% unless current_variant.available %} disabled="disabled"{% endunless %}>
                  {%- render 'icon-cart' with add_class: 'button__icon' -%}
                  <span class="fw-bold">{{ cart_text }}</span>
                </button>
              </div>
              <input type="hidden" name="id" data-productid="{{ product.id }}" value="{{ product.selected_or_first_available_variant.id }}" class="variant-id">
            </div>
          </div>
        </div>
      </div>
      <a class="sc__close sc__close--working text-center hidden-xs hidden-sm" href="javascript:void(0);">
        <div class="hidden-xs hidden-sm not-loading">
          <div class="sc__close__active sc__close__active--desktop">
            {% render 'icon-arrow-down' %}
          </div>
          <span class="sc__close__inactive text-uppercase fw-bold">{{ 'products.product.quick_buy' | t }}</span>
        </div>
      </a>
      <div class="visible-xs visible-sm sc__mobile db w100">
        <a class="sc__trigger sc__trigger--close text-center ver-sub por h100" href="#" title="{{ 'general.accessibility.close_modal' | t | escape }}">{%- render 'icon-close' -%}</a>
        <div class="flex flex-align-ver sc__trigger--open">
          <div class="sc__small-price product-price__price col-xs-3 text-right no-gutter">{%- render 'price' with current_variant.price -%}</div>
          <div class="col-xs-6 flex flex-align-ver flex-align-hoz" style="padding:0 10px;">
            <button class="button add-cart-btn button--single-cart button--single-sticky button--style1 w100 distance-top-ele add-cart-btn--state button--one-line flex flex-align-hoz flex-align-ver" type="submit" name="add" title="{{ 'products.product.add_to_cart' | t | escape }}" aria-label="button" style="margin:0;height:43px;font-size:13px;"{% unless current_variant.available %} disabled="disabled"{% endunless %}>
              {% render 'icon-cart' with add_class: 'sc__cart-icon button__icon' %}
              <span class="fw-bold">{{ cart_text }}</span>
            </button>
          </div>
          <div class="sc__variant-title col-xs-3 text-left no-gutter">
            <a class="sc__trigger w100 dib" title="{{ 'general.buttons.edit' | t | escape }}" href="#">
              {%- unless settings.enable_rtl and settings.rtl_language_codes contains request.locale.iso_code -%}
                {% render 'icon-edit' %}
              {%- endunless -%}
              {%- unless product.has_only_default_variant -%}<span class="pg__variant-title">{{ current_variant.title }}</span>{%- else -%}<span>{{ 'general.buttons.edit' | t }}</span>{%- endunless -%}
              {%- if settings.enable_rtl and settings.rtl_language_codes contains request.locale.iso_code -%}
                {% render 'icon-edit' %}
              {%- endif -%}
            </a>
          </div>
        </div>
      </div>
    {%- endform -%}
  </div>
  {%- endif -%}
</div>
{%- endif -%}

{%- if sizechart and sizechart != 'inline' -%}
<div class="modal fade sizeChartModal sizeChartModal{{ section.id }} product-page-modal" tabindex="-1" role="dialog" id="sizeChartModal{{ section.id }}">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" data-dismiss="modal" aria-label="Close" class="close">{% render 'icon-close' %}</button>
      </div>
      <div class="modal-body">
        <div class="rte">
          {{ sizechart }}
        </div>
      </div>
    </div>  
  </div>  
</div>  
{%- endif -%}
{%- if has_popup_content -%}
<div class="modal fade productPopupModal productPopupModal{{ section.id }} product-page-modal" tabindex="-1" role="dialog" id="productPopupModal{{ section.id }}">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" data-dismiss="modal" aria-label="Close" class="close">{% render 'icon-close' %}</button>
      </div>
      <div class="modal-body">
        <h2 style="margin-top:0;">{{ popup_title }}</h2>
        <div class="rte">
          {{ popup_content }}
        </div>
      </div>
    </div>  
  </div>  
</div>  
{%- endif -%}
<style>.pg__collapse-tab__title--{{ section.id }}{font-size:{{ section.settings.collapse_tab_fs_mb }}px;}@media(min-width:992px){.pg__collapse-tab__title--{{ section.id }}{font-size:{{ section.settings.collapse_tab_fs_gk }}px;} }
{%- if section.settings.sticky_content != 'none' or layout_name == 'ver-images-scrolling' -%}
  @media(min-width:992px) {
    .pos-sticky{position:sticky;position:-webkit-sticky;top:10px;{%- if layout_name == 'ver-images-scrolling' -%}height:100vh;{%- endif -%} }
  }
{%- endif -%}
{%- if layout_name == 'ver-images-scrolling' -%}
  {%- if settings.header_style != 'style1' and settings.header_style != 'style5' and settings.header_style != 'style6' and settings.header_style != 'style8' and settings.header_style != 'style9' and settings.header_style != 'style10' -%}
    @media(min-width:768px){.header-sticker{position:absolute;}.header--desktop:not(.active){background:transparent!important;}{%- if settings.header_style == 'style4' -%}.product-template-section{padding-top:50px;}{%- endif -%} }
  {%- endif -%}
{%- elsif layout_name == 'hoz-images-scrolling' -%}
  .pg__main-options--border hr:first-child {display:none;}
  .pg__main-options--border {
    margin: 0 -15px;
  }
  
  @media(min-width: 375px) {
    .pg__main-options--border {
      padding: 15px;
      border:1px solid rgba(var(--color_general_rgb), 0.1);
      margin: 0;
    }
    .pg__main-options__border-col {
      padding: 0;
    }
  }
  @media(min-width: $min_tablet_large_width) {
    .pg__main-options--border {
      padding: 7px;
    }
  }
  @media(min-width: $desktop_width) {
    .pg__main-options--border {
      padding: 15px;
    }
  }
  .pg__main--scroll-hoz {
    min-width:70%;
  }
  .pg__mains--scroll-hoz{
    overflow-x:auto;
    flex-flow: nowrap;
    white-space: nowrap;
  }
  @media(min-width:1024px) {
    .pg__main--scroll-hoz{
      min-width:400px;
    }  
  }

{%- endif -%}
{%- if layout_name == 'ver-images-scrolling' or section.settings.large_images_grid -%}
  @media(max-width:991px){.scroll-touch-mobile-main{display:flex;flex-flow:row nowrap;-webkit-overflow-scrolling: touch;-moz-overflow-scrolling: touch;-ms-overflow-scrolling: touch;-webkit-overflow-scrolling: touch;scroll-behavior: smooth;-webkit-scroll-behavior: smooth;overflow-x:auto;overflow-y:hidden;}.scroll-touch-mobile-main .pg__main{width:60%;min-width:60%;} }
{%- endif -%}
{%- if layout_name != 'normal' or section.settings.large_images_grid -%}
  .pg__main--{{ section.id }}.active .button--media-single{color:var(--color_link_alternative);}
{%- endif -%}
</style>
{%- if template.suffix != 'quick_view' -%}
  {%- render 'product-single-meta-offer' -%}
{%- endif -%}

{% schema %}
  {
    "name": "Main product - Drag-drop",
    "templates": ["product"],
    "tag": "section",
    "class": "section-product-main",
    "settings": [
      {
        "type": "paragraph",
        "content": "[View the guide for this section](https://beloria-docs.boostheme.com/static-sections/product-page)"
      },
      {
        "type": "header",
        "content": "General settings"
      },
      {
        "type": "checkbox",
        "id": "enable_zoom",
        "label": "Enable image zoom",
        "default": true
      },
      {
        "type": "header",
        "content": "Layouts"
      },
      {
        "type": "radio",
        "id": "layout",
        "label": "Pick a layout",
        "options": [
          {
            "value": "normal",
            "label": "Normal"
          },
          {
            "value": "ver-images-scrolling",
            "label": "Vertical images scrolling"
          },
          {
            "value": "hoz-images-scrolling",
            "label": "Horizontal images scroling"
          }
        ],
        "default": "normal"
      },
      {
        "type": "header",
        "content": "Product Images"
      },
      {
        "type": "checkbox",
        "id": "large_images_grid",
        "label": "Show large images in a grid layout",
        "info": "Only apply in the normal style layout.",
        "default": false
      },
      {
        "type": "checkbox",
        "id": "enable_small_images",
        "label": "Show small images",
        "default": true
      },
      {
        "type": "checkbox",
        "id": "hide_small_images_mb",
        "label": "Hide small images on mobile",
        "default": true
      },
      {
        "type": "checkbox",
        "id": "show_media_btn",
        "label": "Show media button",
        "default": true
      },
      {
        "type": "select",
        "id": "thumbs_layout",
        "label": "Display small images as",
        "options": [
          {
            "value": "hoz_slider",
            "label": "Horizontal slider"
          },
          {
            "value": "ver_slider",
            "label": "Vertical slider"
          },
          {
            "value": "grid",
            "label": "Grid layout"
          }
        ],
        "default": "hoz_slider"
      },
      {
        "type": "select",
        "id": "sticky_content",
        "label": "Sticky content",
        "options": [
          {
            "value": "images",
            "label": "images"
          },
          {
            "value": "information",
            "label": "Product information"
          },
          {
            "value": "none",
            "label": "None"
          }
        ],
        "default": "images",
        "info": "Only affect in the \"normal\" layout."
      },
      {
        "type": "header",
        "content": "The sticky add to cart"
      },
      {
        "type": "checkbox",
        "id": "sc_enable",
        "label": "Enable",
        "default": true
      },
      {
        "type": "header",
        "content": "Collapse tabs settings"
      },
      {
        "type": "range",
        "id": "collapse_tab_fs_mb",
        "label": "Font size on the mobile",
        "min": 1,
        "max": 100,
        "default": 14,
        "unit": "px",
        "step": 1
      },
      {
        "type": "range",
        "id": "collapse_tab_fs_gk",
        "label": "Font size on the desktop & fullscreen",
        "min": 1,
        "max": 100,
        "default": 15,
        "unit": "px",
        "step": 1
      }
    ],
    "blocks": [
      {
        "type": "@app"
      },
      {
        "type": "text",
        "name": "Text",
        "settings": [
          {
            "type": "text",
            "id": "text",
            "default": "Text block",
            "label": "Label"
          },
          {
            "type": "select",
            "id": "text_style",
            "options": [
              {
                "value": "normal",
                "label": "Normal"
              },
              {
                "value": "title",
                "label": "Title"
              },
              {
                "value": "vendor",
                "label": "Vendor"
              }
            ],
            "default": "normal",
            "label": "Text style"
          }
        ]
      },
      {
        "type": "title",
        "name": "Title",
        "limit": 1
      },
      {
        "type": "price",
        "name": "Price",
        "limit": 1,
        "settings": [
          {
            "type": "header",
            "content": "Discount text"
          },
          {
            "type": "checkbox",
            "id": "show_discount_text",
            "label": "Show discount text",
            "default": true
          },
          {
            "type": "text",
            "id": "discount_text",
            "label": "Discount text",
            "default": "save [percent]",
            "info": "Tips: use \"[percent]\" variable to show the percent number. Ex: -50%."
          },
          {
            "type": "header",
            "content": "Shipping policy"
          },
          {
            "type": "checkbox",
            "id": "show_shipping_policy_link",
            "label": "Show the shipping policy link",
            "info": "You can add your own shipping policy in Settings > Legal.",
            "default": false
          }
        ]
      },
      {
        "type": "review_stars",
        "name": "Review stars",
        "limit": 1
      },
      {
        "type": "sku",
        "name": "SKU",
        "limit": 1
      },
      {
        "type": "variant_picker",
        "name": "Variant picker",
        "limit": 1
      },
      {
        "type": "quantity_buy_buttons",
        "name": "Quantity & buy buttons",
        "limit": 1,
        "settings": [
          {
            "type": "checkbox",
            "id": "show_quantity_selector",
            "label": "Show quantity selector",
            "default": true
          },
          {
            "type": "checkbox",
            "id": "show_dynamic_checkout",
            "default": true,
            "label": "Show dynamic checkout buttons",
            "info": "Using the payment methods available on your store, customers see their preferred option, like PayPal or Apple Pay. [Learn more](https:\/\/help.shopify.com\/manual\/using-themes\/change-the-layout\/dynamic-checkout)"
          },
          {
            "type": "checkbox",
            "id": "show_compare_btn",
            "default": true,
            "label": "Show compare button"
          },
          {
            "type": "checkbox",
            "id": "show_wishlist_btn",
            "default": true,
            "label": "Show wishlist button"
          },
          {
            "type": "checkbox",
            "id": "enable_shop_pay_banner",
            "label": "Show Shop Pay Installments banner",
            "default": false
          }
        ]
      },
      {
        "type": "description",
        "name": "Description",
        "limit": 1
      },
      {
        "type": "share",
        "name": "Share",
        "limit": 1,
        "settings": [
          {
            "type": "paragraph",
            "content": "If you include a link in social media posts, the page’s featured image will be shown as the preview image. [Learn more](https:\/\/help.shopify.com\/en\/manual\/online-store\/images\/showing-social-media-thumbnail-images)."
          },
          {
            "type": "paragraph",
            "content": "A store title and description are included with the preview image. [Learn more](https:\/\/help.shopify.com\/en\/manual\/promoting-marketing\/seo\/adding-keywords#set-a-title-and-description-for-your-online-store)."
          }
        ]
      },
      {
        "type": "random_sold_number",
        "name": "Random sold number",
        "limit": 1,
        "settings": [
          {
            "type": "range",
            "id": "sold_min",
            "label": "Min sold number",
            "min": 5,
            "max": 100,
            "step": 1,
            "default": 10
          },
          {
            "type": "range",
            "id": "sold_max",
            "label": "Max sold number",
            "min": 5,
            "max": 100,
            "step": 1,
            "default": 50
          },
          {
            "type": "range",
            "id": "sold_hour_min",
            "label": "Min hour number",
            "min": 1,
            "max": 100,
            "step": 1,
            "default": 1
          },
          {
            "type": "range",
            "id": "sold_hour_max",
            "label": "Max hour number",
            "min": 1,
            "max": 100,
            "step": 1,
            "default": 10
          }
        ]
      },
      {
        "type": "countdown_timer",
        "name": "Countdown timer",
        "limit": 1,
        "settings": [
          {
            "type": "text",
            "id": "countdown_text",
            "label": "Text",
            "default": "Limited time only!"
          }
        ]
      },
      {
        "type": "payment_badge",
        "name": "Trusted payment badge",
        "limit": 1,
        "settings": [
          {
            "type": "image_picker",
            "id": "trusted_payment_badge",
            "label": "Trusted payment badge",
            "info": "Leave empty to display the payment icons based on your payment settings."
          }
        ]
      },
      {
        "type": "size_chart",
        "name": "Size chart",
        "limit": 1,
        "settings": [
          {
            "type": "checkbox",
            "id": "use_size_chart_fallback",
            "label": "Use fallback size chart",
            "default": true
          },
          {
            "type": "image_picker",
            "id": "size_chart_fallback",
            "label": "Fallback size chart",
            "info": "Upload your fallback image. This image will be displayed as the size chart of product when a product does not have own size chart in metafields."
          }
        ]
      },
      {
        "type": "limited_stock_text",
        "name": "Limited stock text",
        "limit": 1,
        "settings": [
          {
            "type": "text",
            "id": "ls_text1",
            "label": "The text when quantity is greater than zero",
            "default": "Hurry! – Only [quantity] left in stock.",
            "info": "Only show this text when the quantity is greater than zero. Tip: leave \"[quantity]\" to show the quantity."
          },
          {
            "type": "radio",
            "id": "ls_quantity",
            "label": "The quantity number",
            "options": [
              {
                "value": "real",
                "label": "Real quantity"
              },
              {
                "value": "random",
                "label": "Random"
              }
            ],
            "default": "real"
          },
          {
            "type": "text",
            "id": "low_quantity_threshold",
            "label": "Low quantity threshold",
            "info": "Only show this block if the product's quantity is lower than the threshold.",
            "default": "50"
          },
          {
            "type": "paragraph",
            "content": "------- Random quantity -------"
          },
          {
            "type": "range",
            "id": "ls_quantity_min",
            "label": "Minimum",
            "min": 1,
            "max": 99,
            "step": 1,
            "default": 1
          },
          {
            "type": "range",
            "id": "ls_quantity_max",
            "label": "Maximum",
            "min": 1,
            "max": 100,
            "step": 1,
            "default": 100
          },
          {
            "type": "text",
            "id": "ls_text2",
            "label": "The text without quantity",
            "default": "Hurry, low quantity!",
            "info": "Only show this text when the quantity is zero."
          },
          {
            "type": "color",
            "id": "ls_color_text",
            "label": "The text color",
            "default": "#222"
          },
          {
            "type": "range",
            "id": "ls_fs_text",
            "label": "The text font size",
            "min": 12,
            "max": 30,
            "step": 1,
            "default": 15,
            "unit": "px"
          },
          {
            "type": "paragraph",
            "content": "------- The progress bar -------"
          },
          {
            "type": "range",
            "id": "ls_percent",
            "label": "The percent number in the progress bar",
            "min": 0,
            "max": 100,
            "step": 1,
            "default": 70,
            "info": "Leave zero to hide the progress bar."
          },
          {
            "type": "color",
            "id": "ls_progress_bar_bkg_complete",
            "label": "The background color of completed area",
            "default": "#151313"
          },
          {
            "type": "color",
            "id": "ls_progress_bar_bkg_remain",
            "label": "The background color of remain area",
            "default": "#dcdcdc"
          },
          {
            "type": "range",
            "id": "ls_progress_bar_height",
            "label": "The height of progress bar",
            "min": 1,
            "max": 45,
            "step": 1,
            "default": 5,
            "unit": "px"
          }
        ]
      },
      {
        "type": "store-availability",
        "name": "Store availability",
        "limit": 1
      },
      {
        "type": "collections-tags",
        "name": "Collection & tags",
        "limit": 1,
        "settings": [
          {
            "type": "checkbox",
            "id": "show_collections",
            "label": "Show collections",
            "default": true
          },
          {
            "type": "checkbox",
            "id": "show_tags",
            "label": "Show product tags",
            "default": true
          }
        ]
      },
      {
        "type": "custom_field",
        "name": "Custom field",
        "settings": [
          {
            "type": "text",
            "id": "label_field_1",
            "label": "Label"
          },
          {
            "type": "select",
            "id": "type_field_1",
            "label": "Type",
            "options": [
              {
                "value": "text",
                "label": "Text - short"
              },
              {
                "value": "textarea",
                "label": "Text - long"
              },
              {
                "value": "radio",
                "label": "Radio buttons"
              },
              {
                "value": "select",
                "label": "Drop-down select"
              },
              {
                "value": "checkbox",
                "label": "Checkbox"
              },
              {
                "value": "checkbox_group",
                "label": "Checkbox group"
              },
              {
                "value": "file",
                "label": "File uploader"
              }
            ],
            "default": "text"
          },
          {
            "type": "textarea",
            "id": "options_field_1",
            "label": "Option values",
            "info": "Options if using radio buttons, a drop-down select, or checkbox group. Separate your options with a comma."
          },
          {
            "type": "text",
            "id": "accepted_types_field_1",
            "label": "Accepted types",
            "info": "Specifies a filter for what file types the user can pick from the file input dialog box. Syntax: \"file_extension|audio/*|video/*|image/*|media_type\"."
          },
          {
            "type": "checkbox",
            "id": "required_field_1",
            "label": "Required",
            "default": false
          },
          {
            "type": "text",
            "id": "error_msg_field_1",
            "label": "Required message",
            "info": "Only work if the field is required."
          }
        ]
      },
      {
        "type": "custom_fields_group",
        "name": "Custom fields group",
        "limit": 1,
        "settings": [
          {
            "type": "paragraph",
            "content": "======= Field 1 ========"
          },
          {
            "type": "checkbox",
            "id": "enable_field_1",
            "label": "Enable"
          },
          {
            "type": "text",
            "id": "label_field_1",
            "label": "Label"
          },
          {
            "type": "select",
            "id": "type_field_1",
            "label": "Type",
            "options": [
              {
                "value": "text",
                "label": "Text - short"
              },
              {
                "value": "textarea",
                "label": "Text - long"
              },
              {
                "value": "radio",
                "label": "Radio buttons"
              },
              {
                "value": "select",
                "label": "Drop-down select"
              },
              {
                "value": "checkbox",
                "label": "Checkbox"
              },
              {
                "value": "checkbox_group",
                "label": "Checkbox group"
              },
              {
                "value": "file",
                "label": "File uploader"
              }
            ],
            "default": "text"
          },
          {
            "type": "textarea",
            "id": "options_field_1",
            "label": "Option values",
            "info": "Options if using radio buttons, a drop-down select, or checkbox group. Separate your options with a comma."
          },
          {
            "type": "text",
            "id": "accepted_types_field_1",
            "label": "Accepted types",
            "info": "Specifies a filter for what file types the user can pick from the file input dialog box. Syntax: \"file_extension|audio/*|video/*|image/*|media_type\"."
          },
          {
            "type": "checkbox",
            "id": "required_field_1",
            "label": "Required",
            "default": false
          },
          {
            "type": "text",
            "id": "error_msg_field_1",
            "label": "Required message",
            "info": "Only work if the field is required."
          },
          {
            "type": "paragraph",
            "content": "======= Field 2 ========"
          },
          {
            "type": "checkbox",
            "id": "enable_field_2",
            "label": "Enable"
          },
          {
            "type": "text",
            "id": "label_field_2",
            "label": "Label"
          },
          {
            "type": "select",
            "id": "type_field_2",
            "label": "Type",
            "options": [
              {
                "value": "text",
                "label": "Text - short"
              },
              {
                "value": "textarea",
                "label": "Text - long"
              },
              {
                "value": "radio",
                "label": "Radio buttons"
              },
              {
                "value": "select",
                "label": "Drop-down select"
              },
              {
                "value": "checkbox",
                "label": "Checkbox"
              },
              {
                "value": "checkbox_group",
                "label": "Checkbox group"
              },
              {
                "value": "file",
                "label": "File uploader"
              }
            ],
            "default": "text"
          },
          {
            "type": "textarea",
            "id": "options_field_2",
            "label": "Option values",
            "info": "Options if using radio buttons, a drop-down select, or checkbox group. Separate your options with a comma."
          },
          {
            "type": "text",
            "id": "accepted_types_field_2",
            "label": "Accepted types",
            "info": "Specifies a filter for what file types the user can pick from the file input dialog box. Syntax: \"file_extension|audio/*|video/*|image/*|media_type\"."
          },
          {
            "type": "checkbox",
            "id": "required_field_2",
            "label": "Required",
            "default": false
          },
          {
            "type": "text",
            "id": "error_msg_field_2",
            "label": "Required message",
            "info": "Only work if the field is required."
          },
          {
            "type": "paragraph",
            "content": "======= Field 3 ========"
          },
          {
            "type": "checkbox",
            "id": "enable_field_3",
            "label": "Enable"
          },
          {
            "type": "text",
            "id": "label_field_3",
            "label": "Label"
          },
          {
            "type": "select",
            "id": "type_field_3",
            "label": "Type",
            "options": [
              {
                "value": "text",
                "label": "Text - short"
              },
              {
                "value": "textarea",
                "label": "Text - long"
              },
              {
                "value": "radio",
                "label": "Radio buttons"
              },
              {
                "value": "select",
                "label": "Drop-down select"
              },
              {
                "value": "checkbox",
                "label": "Checkbox"
              },
              {
                "value": "checkbox_group",
                "label": "Checkbox group"
              },
              {
                "value": "file",
                "label": "File uploader"
              }
            ],
            "default": "text"
          },
          {
            "type": "textarea",
            "id": "options_field_3",
            "label": "Option values",
            "info": "Options if using radio buttons, a drop-down select, or checkbox group. Separate your options with a comma."
          },
          {
            "type": "text",
            "id": "accepted_types_field_3",
            "label": "Accepted types",
            "info": "Specifies a filter for what file types the user can pick from the file input dialog box. Syntax: \"file_extension|audio/*|video/*|image/*|media_type\"."
          },
          {
            "type": "checkbox",
            "id": "required_field_3",
            "label": "Required",
            "default": false
          },
          {
            "type": "text",
            "id": "error_msg_field_3",
            "label": "Required message",
            "info": "Only work if the field is required."
          },
          {
            "type": "paragraph",
            "content": "======= Field 4 ========"
          },
          {
            "type": "checkbox",
            "id": "enable_field_4",
            "label": "Enable"
          },
          {
            "type": "text",
            "id": "label_field_4",
            "label": "Label"
          },
          {
            "type": "select",
            "id": "type_field_4",
            "label": "Type",
            "options": [
              {
                "value": "text",
                "label": "Text - short"
              },
              {
                "value": "textarea",
                "label": "Text - long"
              },
              {
                "value": "radio",
                "label": "Radio buttons"
              },
              {
                "value": "select",
                "label": "Drop-down select"
              },
              {
                "value": "checkbox",
                "label": "Checkbox"
              },
              {
                "value": "checkbox_group",
                "label": "Checkbox group"
              },
              {
                "value": "file",
                "label": "File uploader"
              }
            ],
            "default": "text"
          },
          {
            "type": "textarea",
            "id": "options_field_4",
            "label": "Option values",
            "info": "Options if using radio buttons, a drop-down select, or checkbox group. Separate your options with a comma."
          },
          {
            "type": "text",
            "id": "accepted_types_field_4",
            "label": "Accepted types",
            "info": "Specifies a filter for what file types the user can pick from the file input dialog box. Syntax: \"file_extension|audio/*|video/*|image/*|media_type\"."
          },
          {
            "type": "checkbox",
            "id": "required_field_4",
            "label": "Required",
            "default": false
          },
          {
            "type": "text",
            "id": "error_msg_field_4",
            "label": "Required message",
            "info": "Only work if the field is required."
          }
        ]
      },
      {
        "type": "collapsible_tab_text",
        "name": "t:sections.main-product.blocks.collapsible_tab.name_custom_text",
        "settings": [
          {
            "type": "paragraph",
            "content": "Only work in the \"normal\" layout."
          },
          {
            "type": "text",
            "id": "heading",
            "default": "Collapsible tab",
            "info": "Include a heading that explains the content.",
            "label": "Heading"
          },
          {
            "type": "richtext",
            "id": "content",
            "label": "Tab content"
          },
          {
            "type": "page",
            "id": "page",
            "label": "Tab content from page"
          },
          {
            "type": "select",
            "id": "default_state",
            "label": "Default state",
            "options": [
              {
                "value": "collapse",
                "label": "Collapse"
              },
              {
                "value": "expand",
                "label": "Expand"
              }
            ],
            "default": "expand"
          },
          {
            "type": "checkbox",
            "id": "show_bottom_line",
            "label": "Show bottom line",
            "default": true
          }
        ]
      },
      {
        "type": "collapsible_tab_desc",
        "name": "t:sections.main-product.blocks.collapsible_tab.name_description",
        "limit": 1,
        "settings": [
          {
            "type": "paragraph",
            "content": "Show the product's description. Only work in the \"normal\" layout."
          },
          {
            "type": "text",
            "id": "heading",
            "default": "Description",
            "info": "Include a heading that explains the content.",
            "label": "Heading"
          },
          {
            "type": "select",
            "id": "default_state",
            "label": "Default state",
            "options": [
              {
                "value": "collapse",
                "label": "Collapse"
              },
              {
                "value": "expand",
                "label": "Expand"
              }
            ],
            "default": "expand"
          },
          {
            "type": "checkbox",
            "id": "show_bottom_line",
            "label": "Show bottom line",
            "default": true
          }
        ]
      },
      {
        "type": "collapsible_tab_reviews",
        "name": "Collapsible tab - Reviews",
        "limit": 1,
        "settings": [
          {
            "type": "paragraph",
            "content": "This tab item will show the product review automatically base on your configuration in Theme Settings > Product Settings > Review app. Only work in the \"normal\" layout."
          },
          {
            "type": "text",
            "id": "heading",
            "default": "Reviews",
            "info": "Include a heading that explains the content.",
            "label": "Heading"
          },
          {
            "type": "select",
            "id": "default_state",
            "label": "Default state",
            "options": [
              {
                "value": "collapse",
                "label": "Collapse"
              },
              {
                "value": "expand",
                "label": "Expand"
              }
            ],
            "default": "expand"
          },
          {
            "type": "checkbox",
            "id": "show_bottom_line",
            "label": "Show bottom line",
            "default": true
          }
        ]
      },
      {
        "type": "collapsible_tab_toolkit",
        "name": "Collapsible tab - Toolkit",
        "limit": 1,
        "settings": [
          {
            "type": "paragraph",
            "content": "Show the tabs for a certain product. These tabs have been managed in the Boostheme Toolkit app. Please refer to [this guide](https://beloria-docs.boostheme.com/shopify-apps/boostheme-toolkit-app#4-edit-custom-tabs) for more details. Only work in the \"normal\" layout."
          },
          {
            "type": "select",
            "id": "default_state",
            "label": "Default state",
            "options": [
              {
                "value": "collapse",
                "label": "Collapse"
              },
              {
                "value": "expand",
                "label": "Expand"
              }
            ],
            "default": "expand"
          },
          {
            "type": "checkbox",
            "id": "show_bottom_line",
            "label": "Show bottom line",
            "default": true
          }
        ]
      },
      {
        "type": "popup",
        "name": "Pop-up",
        "settings": [
          {
            "type": "text",
            "id": "link_label",
            "default": "Pop-up link text",
            "label": "Link label"
          },
          {
            "id": "page",
            "type": "page",
            "label": "Page"
          }
        ]
      },
      {
        "type": "group_1",
        "name": "Group of 2 texts, SKU",
        "limit": 1,
        "settings": [
          {
            "type": "header",
            "content": "Text 1"
          },
          {
            "type": "text",
            "id": "text_1",
            "default": "Text 1",
            "label": "Text 1"
          },
          {
            "type": "select",
            "id": "text_style_1",
            "options": [
              {
                "value": "normal",
                "label": "Normal"
              },
              {
                "value": "title",
                "label": "Title"
              },
              {
                "value": "vendor",
                "label": "Vendor"
              }
            ],
            "default": "normal",
            "label": "Text style"
          },
          {
            "type": "header",
            "content": "Text 2"
          },
          {
            "type": "text",
            "id": "text_2",
            "default": "Content",
            "label": "Text 2"
          },
          {
            "type": "select",
            "id": "text_style_2",
            "options": [
              {
                "value": "normal",
                "label": "Normal"
              },
              {
                "value": "title",
                "label": "Title"
              },
              {
                "value": "vendor",
                "label": "Vendor"
              }
            ],
            "default": "normal",
            "label": "Text style"
          },
          {
            "type": "header",
            "content": "SKU"
          },
          {
            "type": "checkbox",
            "id": "show_sku",
            "label": "Show SKU",
            "default": true
          }
        ]
      },
      {
        "type": "group_2",
        "name": "t:sections.main-product.blocks.group.name_2",
        "limit": 1,
        "settings": [
          {
            "type": "header",
            "content": "Reviews"
          },
          {
            "type": "checkbox",
            "id": "show_review_stars",
            "label": "Show review stars",
            "default": true
          },
          {
            "type": "header",
            "content": "Price"
          },
          {
            "type": "checkbox",
            "id": "show_price",
            "label": "Show price",
            "default": true
          },
          {
            "type": "checkbox",
            "id": "show_discount_text",
            "label": "Show discount text",
            "default": true
          },
          {
            "type": "text",
            "id": "discount_text",
            "label": "Discount text",
            "default": "save [percent]",
            "info": "Tips: use \"[percent]\" variable to show the percent number. Ex: -50%."
          },
          {
            "type": "checkbox",
            "id": "show_shipping_policy_link",
            "label": "Show the shipping policy link",
            "info": "You can add your own shipping policy in Settings > Legal.",
            "default": false
          },
          {
            "type": "header",
            "content": "Countdown timer"
          },
          {
            "type": "checkbox",
            "id": "show_countdown",
            "label": "Show countdown",
            "default": true
          },
          {
            "type": "text",
            "id": "countdown_text",
            "label": "Text",
            "default": "Limited time only!"
          }
        ]
      },
      {
        "type": "group_3",
        "name": "t:sections.main-product.blocks.group.name_3",
        "limit": 1,
        "settings": [
          {
            "type": "checkbox",
            "id": "show_collections",
            "label": "Show collections",
            "default": true
          },
          {
            "type": "checkbox",
            "id": "show_tags",
            "label": "Show product tags",
            "default": true
          },
          {
            "type": "checkbox",
            "id": "show_sharing_icons",
            "label": "Show sharing icons",
            "default": true
          },
          {
            "type": "checkbox",
            "id": "show_payment_badge",
            "label": "Show payment badge",
            "default": true
          },
          {
            "type": "image_picker",
            "id": "trusted_payment_badge",
            "label": "Trusted payment badge",
            "info": "Leave empty to display the payment icons based on your payment settings."
          }
        ]
      }
    ]
  }
{% endschema %}